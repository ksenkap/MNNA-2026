model:
  vocab_size: 10000
  d_model: 256
  n_heads: 8
  n_layers: 6
  d_ff: 1024
  max_seq_len: 512
  dropout: 0.1

training:
  batch_size: 32
  learning_rate: 3e-4
  weight_decay: 0.01
  max_epochs: 10
  warmup_steps: 500
  max_grad_norm: 1.0
  checkpoint_dir: checkpoints/
  log_dir: logs/
